function equalityOfSums(array){

    let leftSum = 0;
    let rightSum = 0;
    let isEqual = false;

    for(let index = 0; index < array.length; i++){
        for (currentIndex = 0; currentIndex < array.length; currentIndex++){

            if(index !== 0){
                let index = Number(array[index]);
                let currentIndex = Number(array[index - 1])
            }
        }
    }

}
equalityOfSums([1, 2, 3, 3] )